# TODO — Israeli Film Network Project

Last updated: 2026-05-30 (session 3)

---

## Active / Immediate

### Pipeline — make movies_db.json the real source of truth for filmographies

- [x] **DONE 2026-06-05 — Enriched movies_db crew + switched connections_report to it.**
  See `docs/plan_pipeline_refactor.md` for the full 5-phase record. Summary: built
  `scripts/enrich_movies_db_crew.py` (edb_persons + fund film-page passes, crew 18.5k→~50k);
  `resolve_entities.py` filmography now reads movies_db via `person_films()`; split into
  `--phase resolve|render|all` (acyclic, no stale movies_db); added `Makefile` orchestrator
  + `scripts/validate_stage.py`. QA fix T53 (cross-source film dedup). Residual: ~89 people
  with film careers absent from movies_db crew (mention-only) — acceptable coverage gap.

<details><summary>(original task detail)</summary>

- [ ] **Enrich movies_db.json crew to full crew, THEN switch connections_report to it**
  - **Why:** `connections_report.html` re-derives each person's filmography from raw
    mention URLs, so it diverges from films.html and graph.html (both read
    movies_db.json). A rewrite that sources the report's filmography from
    movies_db.json is ready in `resolve_entities_v2.py` (helpers `_build_movies_index`
    + `person_films`, and the rewritten "קרדיטים בסרטים" block in render_person_card).
  - **Blocker found via A/B (2026-06-04):** movies_db.json crew is director/producer-level
    only — **median 1 crew/film, avg 2.12**. Switching now loses ~5,820 displayed credits
    (8,563 → 3,171) because non-headline roles (editor, sound, dop) aren't in `crew[]`.
    Example: שכבות lists only its director; sound editor ליאון אדרי is absent.
  - **Plan:** write `scripts/enrich_movies_db_crew.py` to backfill full crew into
    movies_db.json (match film by normalized title + year; add missing films too) so
    movies_db becomes the true superset. Pull crew from, in priority order:
      1. `data/cinemaofisrael/raw_films.json` — 1,240 films with full crew (avg 22.5);
         build_movies_db.py is currently dropping this during merge.
      2. EDB person pages — `edb_persons.json` (1,803 persons' full filmographies).
         A big share of real non-headline credits (sound/dop/editor) come from EDB
         `/name/` pages, which the film-page-sourced crew misses.
      Then promote resolve_entities_v2.py and re-run the A/B to confirm ~zero real loss.
  - **Loss-quality verdict (A/B #4, 2026-06-04):** of 4,875 displayed credits the switch
    would drop, ~**85%+ are real** (mostly "film in db but crew too thin"), <**9%** flagged
    "likely false" — and spot-checks showed even those trace to EDB person-page
    filmographies (real), so true contamination is small. The mention path's richness is
    trustworthy; the gap is real data movies_db doesn't hold yet.
  - **Index bug already fixed in v2:** `_build_movies_index` skipped the 1,782 records
    (20%) with no `film_id` (9,083 crew rows); now falls back to a `t|title|year` key.
  - A/B harness: `/tmp/ab_diff.py`, `/tmp/ab_quality2.py`; baseline copy: `resolve_entities_v2.py`.

</details>

### Data enrichment

- [ ] **EDB fund enrichment** — Scrape "תמיכה" section from each film's `/title/{id}/` page → populate `funds` field in `edb_films.json`
  - Plan: `docs/plan_edb_fund_enrichment.md`
  - Script to write: `scripts/edb/enrich_edb_funds.py`
  - Blocker: inspect 2-3 live EDB pages first to find fund section HTML selector
  - After done: add `fund_recipient` edges in `build_network_graph()` (code in plan)

- [ ] **Comprehensive movie database (multi-source)** — Merge film data from EDB + JFC + cinemaofisrael into `data/movies_db.json`
  - Plan: `docs/plan_movie_db.md`
  - Scripts to write:
    - `scripts/edb/enrich_edb_metadata.py` — funds, description, festivals from EDB main pages
    - `scripts/jfc/scrape_jfc.py` — ~1,100 films from `jfc.org.il/movie-sitemap.xml`
    - `scripts/cinemaofisrael/scrape_cinemaofisrael.py` — full cast + crew from ספר הקולנוע הישראלי
    - `scripts/build_movies_db.py` — merge all three sources by title_he + year
  - Expected: 2,000–4,000 unique films; 2,000+ with synopsis; ~500–800 with fund data
  - Blocker: inspect 3 live pages per source before finalizing selectors

- [ ] **Expand EDB film ID collection** — fetch documentary/animation/TV browse pages; update `data/edb/edb_film_ids.json`

---

## Visualization (primary deliverable)

- [x] **Interactive network graph** — `graph.html` rebuilt with **Cytoscape.js + fcose** layout (same stack as reference design)
  - Conflict subgraph: `conflict_graph.json` (314 nodes: 200 people + 8 funds + 5 schools + 101 films, 660 edges)
  - Generated by: `python3 scripts/build_conflict_graph.py` → `conflict_graph.json`
  - Serve: `python3 -m http.server 8000` → http://localhost:8000/graph.html
  - Node shapes: people = ellipse (salmon/red/orange), funds = blue rectangle, schools = purple rectangle, films = dark diamond
  - Edge types on by default: institutional (red), film_funded (amber), affiliated_with (blue)
  - Edge types off by default: crew_credit (green), collaborated_with, co_credited
  - Click node → fades everything else, fills right detail panel with grouped connections
  - Terminology: "ניגוד עניינים" (not "עימות") throughout
  - Planned: filter by specific fund, export selected subgraph, timeline slider

---

## Data gaps

- [ ] **Sapir / Minshar / Beit Berl school alumni** — wiki intro_snippet patterns didn't fire for these schools; would require scraping school websites
- [ ] **EDB critics scraper** (Phase 2 in original plan) — scrape `out/edb_critics/` blog for critic→film edges; already stubbed in `out/edb_critics/mentions.jsonl`
- [ ] **Funding amounts** (Phase 4) — enrich `fund_recipient` edges with actual grant amounts from fund annual reports

---

## Pipeline / resolve_entities.py

- [ ] **Wire `fund_recipient` edges** — after `edb_films.json` has `funds` field; code snippet ready in `docs/plan_edb_fund_enrichment.md` (around line 4060 of `resolve_entities.py`)
- [ ] **Switch to `movies_db.json`** — once `scripts/edb/scrape_edb_movie_db.py` runs, update `build_network_graph()` to read `movies_db.json` instead of `edb_films.json`

---

## Done ✓

- [x] **JFF 2025 (42nd festival) winners + Wikipedia historical winners list** — added (2026-06-03)
  - `sources/jff/pages/jff__he_D7_9E_D7_90_D7_9E_D7_A8_87073__cba4f769.{md,meta.json}` — JFF 2025 winners (חמצן, מלאכי, נאנדאורי, הים, etc.)
  - `sources/jff/pages/jff__wiki_..._ea1937f1.{md,meta.json}` — full historical winners 1989-2025 (חג'ג' עלילתי, ואן ליר/דיאמונד תיעודי)
  - Both fetched via r.jina.ai
  - Picked up automatically on next `resolve_entities.py` run

- [x] **Ministry of Culture 2024 cinema prizes** — added gov.il/he/pages/praasim_2024_7 (2026-06-03)
  - Saved to `sources/ministry_of_culture/pages/ministry_of_culture__he_pages_praasim_2024_7__919e64b4.{md,meta.json}`
  - Fetched via r.jina.ai (gov.il blocked by Cloudflare for crawl4ai service)
  - Winners: רנן שור (מפעל חיים), קובי פרג', אוהד מילשטיין, רועי אסף (עידוד היצירה), רוני בהט, כליל כובש (יוצרים בראשית דרכם)
  - Jury: יעל פרלוב (יו"ר), אלירן מלכה, יצחק צחייק, שמוליק דובדבני, נועה ברגמן-הרצברג, יוסי מדמוני, גליה בדור; משקיף: רמי שלמור
  - Picked up automatically on next `resolve_entities.py` run

- [x] **Derived conflicts pipeline** — 7 new conflict patterns detected and integrated (2026-05-30)
  - `scripts/compute_derived_conflicts.py` → `data/derived_conflicts.json`
  - New edge types in graph: critic_committee (39), family_conflict (6), collab_committee (50), festival_committee (61)
  - New section "ניגודי עניינים נגזרים" in `connections_report.html` (5 subsections)
  - 96 nodes now marked `has_derived_conflict` in `conflict_graph.json`
  - Pattern catalogue: `docs/conflict_patterns.md` (8 future patterns also documented)

- [x] EDB Phase 1a — collect all film IDs → `edb_film_ids.json`
- [x] EDB Phase 1b — scrape each film's crew → `edb_films.json` (285 films)
- [x] EDB Phase 1c — export to `out/edb/mentions.jsonl`
- [x] EDB Phase 3 — person credits (actors + extra) → `edb_persons.json` (1,803 persons)
- [x] `resolve_entities.py` Phase 5 — film nodes, co_credited, crew_credit, reviewed, studied_at edges
- [x] EDB critics `reviewed` edges (424 edges, 7 critics)
- [x] School alumni `studied_at` edges (9 edges: 6 TAU, 3 Sam Spiegel)
- [x] Film council OSINT — 21-member council, conflict flags for עמיחי חסון, ענבל שוקי
- [x] שירה מרגלית EDB films bug fix (T37 added to QA_TESTS.md)
- [x] T34 QA test fixed (SOURCE_COLORS → data-src attribute approach)
- [x] **graph.html** — full redesign to Cytoscape.js + fcose (session 2026-05-30)
  - Migrated from Sigma.js + spring_layout (hairball) to Cytoscape + fcose (overlap-aware)
  - `scripts/build_conflict_graph.py` — new script builds `conflict_graph.json` from network_graph.json + edb_films.json + faculty_members.json
  - Ran `scripts/importers/import_film_faculty.py` + `import_haifa_film_festival.py` to populate out/ mentions
  - Added 5 school nodes (TAU, Sam Spiegel, Sapir, Minshar, Beit Berl), 17 faculty edges, 101 film nodes, 132 crew_credit edges
  - Design language: Heebo font, GitHub dark palette (#0d1117), reference-matching CSS variables
